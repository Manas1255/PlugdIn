import 'package:flutter/material.dart';

class EventFiltersView extends StatelessWidget {
  const EventFiltersView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

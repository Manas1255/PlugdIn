import 'package:flutter/material.dart';

class CaterersFilterView extends StatelessWidget {
  const CaterersFilterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/material.dart';

class PhotographersFilterView extends StatelessWidget {
  const PhotographersFilterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

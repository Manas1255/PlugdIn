import 'package:flutter/material.dart';

class VendorPostScreen extends StatelessWidget {
  const VendorPostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Post Screen',
        ),
      ),
    );
  }
}

